<%= form_for [@dataset, @record] do |f| %>  
  <div class="field form-group">
    <!-- <%#= f.label :pdf, "Upload File" %><br> -->
    <%= f.file_field :pdf, :class => 'btn btn-primary btn-sm' %>
  </div>

  <div class="actions">
    <%= f.submit "Upload", :class => 'btn btn-success' %>
  </div>
<% end %>
